name: ygo_card_deck
description: "YGOPRO card CDN and deck square API client — card/deck resource interface."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  http: ^1.2.1

dev_dependencies:
  test: ^1.24.0
  flutter_lints: ^6.0.0
