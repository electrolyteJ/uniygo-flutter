name: resource_banlist_mycard
description: "YGOPRO banlist / forbidden-limited list service — MyCard CDN backend."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  http: ^1.2.1
  resource_data:
    path: ../resource_data
  service_loader:
    path: ../service_loader

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
