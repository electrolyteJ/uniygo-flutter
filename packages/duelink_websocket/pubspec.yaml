name: duelink_websocket
description: WebSocket implementation for duelink online duels.
version: 0.0.1
environment:
  sdk: ^3.12.2
resolution: workspace
publish_to: 'none'

dependencies:
  applog:
    path: ../applog
  flutter:
    sdk: flutter
  duelink:
    path: ../duelink
  web_socket_channel: ^3.0.0
  async: ^2.11.0
  service_loader:
    path: ../service_loader


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
