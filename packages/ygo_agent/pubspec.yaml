name: ygo_agent
description: >-
  Dart port of ygo-agent's ygoinf feature pipeline (features.py) for
  bit-exact on-device inference with the 0546_26550M tflite model.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.12.2

resolution: workspace

dev_dependencies:
  archive: ^4.0.0
  flutter_lints: ^6.0.0
  test: ^1.24.0
