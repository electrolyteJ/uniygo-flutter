name: biz
description: "A new Flutter project."
version: 0.0.1
homepage:
resolution: workspace
environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"
publish_to: 'none'
dependencies:
  flutter:
    sdk: flutter
  service_loader:
    path: ../service_loader
  duelink:
    path: ../duelink
  duelink_websocket:
    path: ../duelink_websocket
  duelink_socket:
    path: ../duelink_socket
  duelink_ai:
    path: ../duelink_ai
  duelink_ai_edo:
    path: ../duelink_ai_edo
  resource_data:
    path: ../resource_data
  resource_card_baige:
    path: ../resource_card_baige
  resource_card_mycard:
    path: ../resource_card_mycard
  resource_banlist_mycard:
    path: ../resource_banlist_mycard
  resource_strings_mycard:
    path: ../resource_strings_mycard
  resource_deck_mycard:
    path: ../resource_deck_mycard
  flutter_riverpod: ^3.4.2
  shared_preferences: ^2.5.3
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.2
  audioplayers: ^6.8.1
  riverpod_annotation: ^4.0.6
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  riverpod_generator: ^4.0.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
