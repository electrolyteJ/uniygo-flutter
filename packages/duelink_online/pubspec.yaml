name: duelink_online
description: WebSocket implementation for duelink online duels.
version: 0.0.1
environment:
  sdk: ^3.12.2
resolution: workspace
publish_to: 'none'

dependencies:
  flutter:
    sdk: flutter
  duelink:
    path: ../duelink
  web_socket_channel: ^3.0.0
  async: ^2.11.0

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^6.0.0
