name: resource_strings_mycard
description: "YGOPRO strings.conf（引擎提示文案）服务 — MyCard CDN backend."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  applog:
    path: ../applog
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
