name: service_loader_gen
description: "service_loader 的编译时代码生成器，根据 @ServiceRegister 注解自动生成注册函数。"
version: 0.0.1
resolution: workspace
publish_to: none
environment:
  sdk: ^3.12.2

dependencies:
  analyzer: ^12.1.0
  build: ^4.0.7
  dart_style: ^3.1.0
  glob: ^2.1.0
  package_config: ^2.2.0
  pub_semver: ^2.1.0
  service_loader:
    path: ../service_loader
  source_gen: ^4.2.4
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.25.0
