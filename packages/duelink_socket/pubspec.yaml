name: duelink_socket
description: TCP Socket implementation for duelink duels.
version: 0.0.1
environment:
  sdk: ^3.12.2
resolution: workspace
publish_to: 'none'

dependencies:
  flutter:
    sdk: flutter
  duelink:
    path: ../duelink
  async: ^2.11.0
  service_loader:
    path: ../service_loader


dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^6.0.0
