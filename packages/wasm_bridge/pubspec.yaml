name: wasm_bridge
description: "C/C++ 到 Flutter web 的桥接工具：编译 wasm/js 并校验导出符号，生成 Flutter web 插件（动态注入 wasm/js 脚本）和 dart:js_interop 接口声明。"
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.29.0

executables:
  wasm_bridge:
