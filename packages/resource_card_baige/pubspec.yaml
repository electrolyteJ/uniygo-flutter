name: resource_card_baige
description: 百鸽 (ygocdb.com) card API client — card query and image CDN service.
version: 1.0.0
resolution: workspace
publish_to: 'none'
environment:
  sdk: ^3.12.2

dependencies:
  http: ^1.2.1
  resource_data:
    path: ../resource_data
  service_loader:
    path: ../service_loader

dev_dependencies:
  test: ^1.24.0
  flutter_lints: ^6.0.0
