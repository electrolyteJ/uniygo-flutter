name: deck_editor1
description: "卡组编辑器：卡组浏览/搜索/编辑 UI（自 uniygopro 下沉，供宿主 app 以包依赖形式复用）。"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  go_router: ^14.0.0
  http: ^1.2.1
  ygo_data:
    path: ../../packages/ygo_data
  biz:
    path: ../../packages/biz
  ygo_card_mycard:
    path: ../../packages/ygo_card_mycard
  ygo_banlist_mycard:
    path: ../../packages/ygo_banlist_mycard

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
